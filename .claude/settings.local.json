{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -q '^/home/user/tour-du-monde/'; then cd /home/user/tour-du-monde && git add -A && git commit -m 'auto-deploy update' && git push origin main; fi; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Auto-deploying..."
          }
        ]
      }
    ]
  }
}
